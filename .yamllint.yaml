# yamllint — semantic/structural YAML checks only (issue #349).
#
# Prettier already owns *formatting* (indentation, line length, spacing, quoting),
# so this starts from the `relaxed` preset and the few style rules left on are
# disabled below with a reason — the linter must never fight the formatter. What we
# DO enforce is the high-signal tail Prettier can't see: duplicate mapping keys, and
# YAML 1.1 "Norway problem" truthy values (yes/no/on/off) used where a real boolean
# was meant. Run non-strict (`yamllint .`), so only these error-level rules gate.
extends: relaxed

rules:
  # Disable the remaining style rules Prettier owns:
  line-length: disable # Prettier wraps; an 80-col cap is not our standard
  document-start: disable # our YAML omits `---`; k8s multi-doc `---` is a separator, not a header

  # High-signal checks we DO enforce (promoted to errors so they gate):
  truthy:
    # Only real booleans as VALUES; flag yes/no/on/off/Yes/No/True/False. check-keys
    # false so GitHub Actions' `on:` trigger key (parsed as the boolean `on`) is fine.
    allowed-values: ['true', 'false']
    check-keys: false
    level: error
  key-duplicates:
    level: error

# gitignore-style patterns (match at any depth): dependency/build trees, the two
# committed-but-generated YAML artifacts, and the vendored/local skill docs.
ignore: |
  node_modules/
  dist/
  coverage/
  pnpm-lock.yaml
  services/graph-service/docs/insomnia.collection.yaml
  .agents/
  .claude/
